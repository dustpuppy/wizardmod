package thewizardmod.jars;

import net.minecraft.entity.item.EntityItem;
import net.minecraft.init.Items;
import net.minecraft.item.ItemStack;
import net.minecraft.tileentity.TileEntity;

public class TileEntityJar extends TileEntity{

	private int counter = 0;
	private int MAX = 8;
	private Item item = Items.APPLE;
	
	public boolean addItem()
	{
		if(counter < MAX)
		{
			counter++;
			return true;
		}
		return false;
	}
	
	public void removeItem()
	{
		if(counter > 0)
		{
			worldObj.spawnEntityInWorld(new EntityItem(worldIn, pos.getX() + 0.5F, pos.getY() + 1, pos.getZ() + 0.5F, new ItemStack(item)));
		}
	}
	
}
